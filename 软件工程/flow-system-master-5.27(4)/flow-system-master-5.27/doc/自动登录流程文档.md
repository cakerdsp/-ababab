# Flow System 自动登录流程文档

## 概述

本文档详细描述了 Flow System 应用的自动登录实现流程，遵循基于 refreshToken 的自动登录标准流程。

## 自动登录流程架构图

```
用户启动应用
      ↓
渲染进程检查自动登录
      ↓
主进程调用AccountManager.initialize()
      ↓
检查本地访问令牌是否存在/有效
      ↓
[无效/不存在] → 使用refreshToken请求新token
      ↓
服务器验证refreshToken
      ↓
[成功] → 返回新的访问令牌 → 保存并完成自动登录
[失败] → 清除本地数据 → 跳转登录页面
```

## 详细流程实现

### 1. 应用启动触发自动登录检查

**文件**: `src/renderer/src/views/login/index.vue`
**行号**: 84-103

```javascript
// 检查自动登录
async checkAutoLogin() {
  try {
    const autoLoginResult = await window.api.checkAutoLogin();
    if (autoLoginResult && autoLoginResult.success) {
      this.$message({
        message: '自动登录成功！',
        type: 'success'
      });

      // 直接跳转到主界面
      const screenWidth = window.screen.width;
      const screenHeight = window.screen.height;
      const calculatedWidth = Math.floor((screenWidth / 5) * 3) + 250;
      const calculatedHeight = 800;

      window.ipcRenderer.send('toMain', {
        username: autoLoginResult.user?.username || 'auto',
        token: autoLoginResult.token || '',
        screenWidth: calculatedWidth,
        screenHeight: calculatedHeight
      });

      this.$router.push('/main');
    }
  } catch (error) {
    console.log('无自动登录或自动登录失败:', error);
  }
}
```

**触发时机**: `created()` 生命周期钩子
**行号**: 79-82

### 2. 预加载脚本API调用

**文件**: `src/preload/index.js`
**行号**: 10

```javascript
checkAutoLogin: () => ipcRenderer.invoke('check-auto-login'),
```

### 3. 主进程IPC处理

**文件**: `src/main/index.js`
**行号**: 92-118

```javascript
// 检查自动登录
ipcMain.handle('check-auto-login', async (event) => {
  try {
    const accountManager = require('../class');
    const result = await accountManager.initialize();

    if (result.success && result.autoLogin) {
      const currentUser = accountManager.getCurrentUser();
      const token = accountManager.dataStorage.getToken();

      return {
        success: true,
        autoLogin: true,
        user: {
          username: currentUser?.getUserName(),
          userId: currentUser?.getUserId()
        },
        token: token
      };
    }

    return { success: false, autoLogin: false };
  } catch (error) {
    console.error('自动登录检查错误:', error);
    return { success: false, message: error.message || '自动登录检查失败' };
  }
});
```

### 4. 账户管理器核心自动登录逻辑

**文件**: `src/class/AccountManager.js`
**行号**: 15-76

#### 4.1 初始化方法入口

```javascript
async initialize() {
  if (this.isInitialized) {
    // 如果已经初始化，返回当前状态
    const isLoggedIn = this.isLoggedIn();
    return { success: isLoggedIn, autoLogin: isLoggedIn };
  }
}
```

#### 4.2 检查本地会话和设置

**行号**: 25-30

```javascript
// 检查是否有有效的会话
if (this.dataStorage.hasValidSession()) {
  const userData = this.dataStorage.getUserAccount();
  const settings = this.dataStorage.getAppSettings();

  if (userData && settings.autoLogin) {
```

#### 4.3 步骤2: 检查本地访问令牌

**行号**: 31-32

```javascript
// 步骤2: 检查本地是否有有效的访问令牌
const token = this.dataStorage.getToken();
```

#### 4.4 步骤3: 使用refreshToken获取新令牌

**行号**: 34-52

```javascript
if (!token) {
骤3: 如果没有有效访问令牌，使用refreshToken请求新的token
  const refreshToken = this.dataStorage.getRefreshToken();
  if (refreshToken) {
    console.log('访问令牌不存在，尝试使用refreshToken获取新令牌');
    const refreshResult = await this.apiRegistry.refreshAccessToken(refreshToken);

    if (refreshResult.success) {
      // 步骤5: 收到新的访问令牌后，保存并使用它
      console.log('成功刷新访问令牌');
      this.currentUser = new UserAccount(userData.username, userData.userId);

      // 获取绑定信息
      await this.loadUserBindings();

      console.log('自动登录成功');
      this.isInitialized = true;
      return { success: true, autoLogin: true };
    }
  }
}
```

#### 4.5 验证现有Token有效性

**行号**: 62-76

```javascript
} else {
  // 有token，直接尝试验证token有效性
  // 通过调用一个需要认证的API来验证token是否有效
  try {
    await this.loadUserBindings(); // 这个API需要认证，会触发token验证
    this.currentUser = new UserAccount(userData.username, userData.userId);
    console.log('使用现有token自动登录成功');
    this.isInitialized = true;
    return { success: true, autoLogin: true };
  } catch (error) {
    // token无效，尝试使用refreshToken
    console.log('现有token无效，尝试使用refreshToken刷新');
    // ... refreshToken逻辑
  }
}
```

### 5. 本地存储会话验证

**文件**: `src/class/AccountStorage.js`
**行号**: 218-225

```javascript
// 检查是否有有效的登录状态
hasValidSession() {
  const token = this.getToken();
  const refreshToken = this.getRefreshToken();
  const userAccount = this.getUserAccount();

  return !!(token && refreshToken && userAccount);
}
```

### 6. RefreshToken 获取

**文件**: `src/class/AccountStorage.js`
**行号**: 117-122

```javascript
// 获取刷新令牌
getRefreshToken() {
  return this.getItem('refresh_token');
}
```

### 7. API调用刷新令牌

**文件**: `src/class/AccountAPIRegistry.js`
**行号**: 143-174

```javascript
// 刷新访问令牌
async refreshAccessToken(refreshToken) {
  try {
    const response = await this.client.post(this.apiPaths.refreshToken, {
      refreshToken: refreshToken
    });

    if (response.data.status === 'success') {
      // 保存新的token
      if (this.storage) {
        this.storage.saveToken(response.data.data.token);
      }
      return {
        success: true,
        data: response.data.data
      };
    } else {
      return {
        success: false,
        message: response.data.message
      };
    }
  } catch (error) {
    console.error('刷新token失败:', error);
    return {
      success: false,
      message: '刷新token失败'
    };
  }
}
```

### 8. 服务器API端点

**API路径**: `POST /auth/refresh-jwt`
**文件定义**: `src/class/AccountAPIRegistry.js` 行号: 18

```javascript
refreshToken: '/auth/refresh-jwt',
```

## 流程特点

1. **符合标准**: 完全遵循 problem.md 中定义的自动登录流程
2. **安全性**: 不直接依赖本地token，而是通过服务器验证refreshToken
3. **容错性**: 多层验证，失败时自动清除无效数据
4. **用户体验**: 自动处理，无需用户干预

## 相关接口

- **refreshToken接口**: `POST http://172.19.15.2:3000/auth/refresh-jwt`
- **请求参数**: `{ refreshToken: string }`
- **响应格式**: 符合接口文档.md规范
